/*
package MultiThreading;

//theory for threading

//automatically runs run when you call start method
//sleep when you want to stop for some time(200)
//wait (),notify()
//stop() to stop the thread

public class thread extends Thread {
    int[] values={6,5,1,7,8};
    public void run();
    {
        for (int i = 0; i < 5; i++) {
            values[i]=values[i]*2;

        }
    }
}


//as multiple inheritance(if we want to extend from two classes at once is not allowed we use runnable instead of threads and implements runnable


public class thread implements Runnable{
    int[] values={6,5,1,7,8};
    public void run();
    {
        for (int i = 0; i < 5; i++) {
            values[i]=values[i]*2;

        }
    }
}
*/
